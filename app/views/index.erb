<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Comedian Index</title>
  </head>
  <body>
    <% @comedians.each do |comedian| %>
    <div>
      <p> Name: <%= comedian.name %></p>
      <p> Age: <%= comedian.age %></p>
      <p> Specials: <%= @specials.select do |special|
                        special.name if special.comedian_id == comedian.id
                        end %></p>
    </div>
    <% end %>
  </body>
</html>
