<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Comedian Index</title>
  </head>
  <body>
    <% @comedians.each do |comedian| %>
    <div>
      <p> Name: <%= comedian.name %></p>
      <p> Age: <%= comedian.age %></p>
      <p> Specials: <% @specials.each do |special| %>
                          <li> <%= special.name if special.comedian_id == comedian.id %></li>
                    <% end %>
    </div>
    <% end %>
  </body>
</html>
