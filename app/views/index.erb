<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Comedian Index</title>
  </head>
  <body>
    <% @comedians.each do |comedian| %>
    <div>
      <p> Name: <%= comedian.name %></p>
      <p> Age: <%= comedian.age %></p>
      <p> Specials: <%= @specials.find_by(comedian.id) %></p>
    </div>
    <% end %>
  </body>
</html>
