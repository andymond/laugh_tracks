<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @location.name %></title>
  </head>
  <body>
    <% if @specials.count >= 2 %>
      <% @specials.each do |special| %>
        <p><%= special.name %></p>
      <% end %>
    <% else %>
      <p>Not enough specials!</p>
    <% end %>
  </body>
</html>
